# Frontend migrated to TypeScript and new design system

- All components and pages are now in TypeScript/TSX.
- UI library and styles are integrated from the provided design.
- See `src/app` for new structure.
